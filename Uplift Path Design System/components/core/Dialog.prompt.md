Modal used mainly for the video lightbox and short forms.

```jsx
<Dialog open={open} onOpenChange={setOpen} style={{maxWidth:"48rem"}}>
  <Card style={{padding:"2rem"}}>
    <DialogTitle>Book a discovery call</DialogTitle>
    <DialogDescription>We'll be in touch within one business day.</DialogDescription>
  </Card>
</Dialog>
```
