# Uplift Path — Relume v2 import

The Relume design for this site has been substantially redesigned: new sections,
new components, new photography. Everything needed is in `v2/` at the repo root:

```
v2/
├─ Uplift Path Design System v2/   the Claude Design bundle (note the spaces)
└─ uplift-export-v2/               the Relume "export to Claude Code" download
```

Import it and rebuild the site on a new branch. **Delete `v2/` as part of the
final cleanup** — it is input, not source, and git keeps it.

Everything below marked **CONFIRMED** was verified against these folders on
2026-08-28. Those are facts about the files on disk, not things to go re-check.

Read `CLAUDE.md` first, then `.claude/skills/uplift-path-design/readme.md`.

## What's in `v2/`

Two artifacts, both from the same Relume revision:

1. **The Relume export** — the "export to Claude Code" download, which the design
   system's readme calls `demo-site-design-uplift-design/`. Contains `DESIGN.md`,
   `sitemap.md`, `assets.md`, `react/globals.css`, `react/@/components/ui/` (13
   primitives) and `react/components/` (~70 sections). **This is the source of
   every page section, and `react/globals.css` is the file `app/globals.css` gets
   re-derived from.**
2. **The design system** — generated by Claude Design from (1). Ships `SKILL.md`
   (frontmatter `name: uplift-path-design`, unchanged from v1), `readme.md`,
   `styles.css`, `tokens/*.css` (8 files), 22 `guidelines/*.html` specimen cards,
   `components/` in three groups, `ui_kits/website/`, `reference/homepage/`
   screenshots, and `assets/` (fonts, logo, images, svgs).

   It does **not** contain the Relume export, a `design-export/` folder, a
   `production.md`, or a `missing-components/` folder — v1 had all four. The
   component map now lives in `readme.md`'s Index section, and the section-by-
   section page map is `sitemap.md` inside the Relume export.

If a third folder is present — a plain **"export React"** download — it is
**reference only. Do not merge it.** It speaks Relume's v3 Tailwind preset
(`bg-background-primary`, `text-md`, `rb-*`), which Tailwind v4 cannot resolve at
all: no error, just silently unstyled. It was discarded in the previous import for
exactly this reason, and was also missing components. Use it only to recover a
component the Claude Code export drops.

**CONFIRMED — both artifacts are present and consistent.** Design system: 48
images, 10 woff2, 10 svgs, 22 guidelines, 10 reference screenshots, 8 token files.
Relume export: `DESIGN.md`, `sitemap.md`, `assets.md`, `react/globals.css` (10 KB),
13 UI primitives in `react/@/components/ui/`, and **78 section files across 18
folders** in `react/components/`. No `missing-components/` folder exists and none
appears to be needed, but cross-check against `sitemap.md` before assuming so.

## Deliverables

1. A new branch, `relume-import-v2`. `master` stays live and untouched throughout.
2. The full site rebuilt on the new design — every route, matching the design
   system's screenshots section for section.
3. **A clean, production-grade repo.** This is an explicit deliverable, not a
   nice-to-have. See below.
4. A green `pnpm build`.

Do not run the `/qa-sweep` or `/seo-audit` agents. The user runs those themselves,
separately, once the branch is up.

## Clean structure

Nothing in the root that isn't config. Specifically:

- `qa-report-2026-08-26.md` — currently loose in the root. Reports belong in
  `docs/`, or delete it (it audits the old design and is now obsolete).
- `MIGRATION.md` — the v1 import playbook, now superseded. Archive under `docs/`
  or remove; git has it either way.
- `package.json` still says `"name": "my-project"`. Fix to `uplift-path-website`.
- `public/placeholder-user.jpg`, `public/placeholder.jpg`, `public/placeholder.svg`
  — v0 leftovers. Confirm nothing references them, then delete.
- These 5 files in `public/images/` are orphans with no reference in the code.
  Re-check against the new build, then drop whatever is still unused:
  `advisory-services-features-list-section-2.jpg`,
  `advisory-services-features-list-section-3.jpg`,
  `advisory-services-features-list-section-4.jpg`,
  `how-we-work-how-it-works-section-new-2.jpg`,
  `page-20-impact-outcomes-new2.jpg`
- Don't invent new top-level folders. The existing layout (`app/`, `components/`,
  `hooks/`, `lib/`, `public/`) is correct — keep it.
- **This file** (`docs/import-v2-prompt.md`) is a one-shot handoff doc. Delete it
  as part of the cleanup once the import is done; git keeps it.

`.gitignore` is already correct; `out/` and `*.tsbuildinfo` are untracked. Leave it.

## Replace vs keep

**Replace wholesale** — delete and re-import, do not merge file by file:

- `components/sections/**`
- `components/ui/**` (if the new export ships primitives)
- `app/*/page.tsx` — rewrite composition to the new section list
- `app/globals.css` — re-derive, see below
- `.claude/skills/uplift-path-design/**` — **replace in place, same path, same
  `name: uplift-path-design` in the frontmatter.** Do not create a parallel "v2"
  skill folder: two skills with near-identical descriptions can't be
  disambiguated, and `CLAUDE.md` points at this one path. v1 is preserved in git.
- `public/images/**` and `public/svgs/**` — the svgs are expected to change too.

**Keep — hand-written or infra, never came from Relume:**

- `lib/utils.ts` — holds an `extendTailwindMerge` fix. The export ships a bare
  `twMerge` as `cn`, which reintroduces a bug where a size class and a tone class
  collide and one silently vanishes. **Do not overwrite this file.**
- `app/layout.tsx` — SEO metadata, favicons, viewport, chrome mounting.
- `hooks/use-media-query.js`
- `next.config.mjs`, `wrangler.jsonc`, `postcss.config.mjs`, `tsconfig.json`
- `.github/workflows/deploy.yml`
- `.claude/agents/`, `.claude/commands/`
- `public/fonts/`, `public/logo/`, `public/brand/` — unless the new design system
  ships different ones, in which case take the new ones.
- `CLAUDE.md` — update it to match the new structure; don't replace it.

**Judgment call:** `components/sections/navbar-05.jsx` is a Relume file, but it
carries hand-written fixes (dropdowns opening simultaneously, a duplicate services
menu). Save the diff first:

    git diff 3f30fe6..HEAD -- components/sections/navbar-05.jsx app/globals.css

Then test whether the *new* navbar has the same bugs, and port the fix only if it
does. Do not blind-apply the patch to a file that changed underneath it.

## Order of operations

1. `git checkout -b relume-import-v2`
2. **Download every `imagedelivery.net` image immediately** — before any other
   work. **CONFIRMED: 53 references in the new export, all signed
   `exp=1788480000` — that is 6 Sep 2026.** The old ones in `components/` expire
   4 Sep. If either lapses the design is unrecoverable and the user has to
   re-export from Relume. Grep for `imagedelivery.net` across `v2/` and
   `components/`, download into `public/images/`, and rewrite the references.
3. Save the navbar + `globals.css` diffs as a patch.
4. Diff the new `sitemap.md` against the current `app/` routes. Produce three
   lists: pages added, pages removed, sections renamed. Show them before copying
   anything — renamed routes are the highest-risk part of this job.
5. Replace sections, ui, skill, images, svgs.
6. Re-derive `app/globals.css`.
7. Rewrite page composition. Add redirects for every changed slug.
8. `pnpm build`, then fix class-name gaps section by section against the screenshots.
9. Update `CLAUDE.md`. Clean the root.

## Known traps — every one of these cost time in the v1 import

**CONFIRMED — `globals.css` still does not compile.** `v2/uplift-export-v2/react/
globals.css` opens `@theme` at line 13 and nests `@layer theme { @media (min-width:
992px) { ... } }` at **line 147, two levels deep inside it**. Tailwind 4 rejects
this outright. Identical to the v1 bug. Fix `[2]` — lift that block out — applies
unchanged.

The current `app/globals.css` carries three fixes marked `[1] [2] [3]` in its
header: self-hosted fonts, that block lifted out, and an `@theme inline` block. All
three still apply. Re-derive with them; never hand-edit token values.

**CONFIRMED — there is no `@theme inline` block in the export.** Zero occurrences.
The design system's `tokens/schemes.css` defines the scheme classes and sets
`--color-scheme-*` correctly, but Tailwind cannot know those custom properties are
*colours*, so `bg-scheme-foreground` / `text-scheme-text` / `border-scheme-border`
generate *nothing* without an `@theme inline` block naming them. It must be
`inline`, or the values freeze instead of resolving per section. Symptom when
missing: every card borderless, every input transparent — silent, no error. Fix
`[3]` applies unchanged.

**v2 has seven schemes, not four, and they're named.** `.scheme-light`,
`.scheme-accent`, `.scheme-navy`, `.scheme-mint`, `.scheme-deep-teal`,
`.scheme-cerulean-deep`, `.scheme-black`, with `.scheme-1/2/3` kept as aliases for
the first three. This resolves a real collision: `DESIGN.md` numbers seven schemes
1–7 and `react/globals.css` numbers three 1–3 **with different meanings** —
DESIGN.md's scheme 4 is white, globals' `scheme-1` is white. Prefer the named
classes; don't renumber anything.

**Relume's exporter drops components.** It refused three in v1 (`Layout613`,
`Layout564`, `Layout615`) over an invalid nested `<img>`; they were recovered into a
`missing-components/` folder. **v2's design system has no such folder** — which
means either nothing was dropped, or nothing was recovered. Cross-check the section
count in `react/components/` against `sitemap.md` before assuming the export is
complete. If something is missing, the discarded "export React" download is the
place to recover it from.

**CONFIRMED — all three v1 export bugs recur, at these exact locations.** Fix them
on the way in; do not "restore" them later.

| Bug | Location in `v2/uplift-export-v2/` |
|---|---|
| `const featureCount = features.length;` — `features` is never defined, crashes the page | `react/components/business-conusltation/layout-486.jsx:16` and `react/components/for-individual-page/layout-486.jsx:16` |
| `tansition-all` (typo, silently dead class) | `react/@/components/ui/textarea.jsx:4` |
| `mb-8border-none` (two classes fused, both dead) | `react/components/business-conusltation/layout-353.jsx` lines 27, 42, 58, 74 |

v1 fixed the `features` crash with a derived `FEATURE_COUNT`; do the same. Note the
affected files shifted — v1 also hit `ai-consultation/layout-485`, which is clean
this time.

**CONFIRMED — `faq-for-test` and `page-20` are still in the export.** The design
system's readme describes the site as 16 pages and doesn't list either, but the
Relume export ships 18 route folders including both. So they aren't dropped — they
just aren't considered real pages. Both look like scratch pages (`faq-for-test` by
name; `page-20` is a Relume default). They're currently live and indexed. Decide
explicitly: ship them, `noindex` them, or delete them with 301s — but surface the
choice, don't recreate them silently because the export happened to include them.

**Section folder names carry Relume's typos** (`business-conusltation`,
`advisory--services`) and the routes match them. If the new export fixes those
spellings, every one is a changed URL on a **live, indexed site** — each needs a
301. Static export means no middleware and no `next.config` redirects: the Worker
is static-assets-only with no `main` script, so use `public/_redirects`, and
verify it actually works on Workers Assets before relying on it.

**Never white text on the teal/green fill** — 1.96:1, fails WCAG AA. Every v2
scheme, green included, sets `--color-scheme-btn-text` to `--color-neutral-darkest`
and already encodes the correct pairing. Don't re-derive it by hand.

**Two `CLAUDE.md` rules are now out of date — update them, don't enforce them.**

- It says *"Cards: 2px border, `rounded-card`, no shadow. This brand has no
  elevation system."* v2 documents one shadow: a hard `0 3px 0 0` ledge under a
  control, zero blur. Still no soft elevation, but the rule as written is wrong.
- It says *"No press-state transforms."* v2's buttons translate down 3px onto that
  ledge on hover, and the readme is explicit that hover and press look identical —
  there is no *separate* press state. Reword rather than delete.

**`--font-weight-bold` is set to `400`.** A "bold" section heading (h2–h6) is
therefore still regular weight, and that is deliberate — Playfair Display 400
carries the headings. Only the hero H1 is heavier (600). Do not "fix" this.

**Photography:** check whether the new images are real client photos or more
Relume stock. The v1 QA report flagged stock placeholders in the navbar mega-menu,
testimonial company logos, and a testimonial avatar. Flag any that remain — don't
silently ship them.

## Definition of done

- `pnpm build` passes clean.
- Every route renders and matches its design-system screenshot.
- No `imagedelivery.net` URL anywhere in the source.
- No unstyled section — spot-check one section per `scheme-N`: no transparent
  cards, no borderless inputs.
- 301s in place for every changed slug.
- Root contains only config; no loose reports.
- `master` untouched. Show the running site and wait for approval before merging.
