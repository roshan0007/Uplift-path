Underlined dropdown. Options are passed as data rather than as children.

```jsx
<Select placeholder="Select one..." options={[{value:"a",label:"First Choice"},{value:"b",label:"Second Choice"}]} />
```
